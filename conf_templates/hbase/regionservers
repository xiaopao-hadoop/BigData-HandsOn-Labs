# ==============================================================================
# File: regionservers
# Description: HBase RegionServer 节点清单模板
# Usage: 
#   1. 将下方的占位符替换为你真实的 Worker 节点主机名
#   2. 严禁包含 localhost
#   3. 每行一个节点，行尾不能有空格，文件末尾不能有空行
# ==============================================================================
<WORKER1_HOSTNAME>
<WORKER2_HOSTNAME>